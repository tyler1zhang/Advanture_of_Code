q1 -- itertools.combination to calculate the total combination
q2 -- regex and split to get the info to calculate
q3 -- enumerate for list that needs index in the loop
q4 -- regex use match to find if pattern exist and return true/false, match multiple character like AND operator. Also match any character with (.|\s) as . does not include new line